spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wliamp/config.git
          username: wliamp
          password: ${GITHUB_TOKEN}
          default-label: main
          search-paths:
            - common
            - apps
server:
  port: 8888
