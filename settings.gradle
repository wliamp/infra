rootProject.name = 'infras'

include 'api-gateway',
    'config-server',
    'identity-provider',
    'identity-provider:authentication',
    'identity-provider:authorization',
    'service-registry'
